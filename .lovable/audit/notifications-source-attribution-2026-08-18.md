# إغلاق دفعة الإشعارات — 2026-08-18

## ما نُفّذ فعليًا (كود فقط)
- اشتقاق مصدر الإشعار خادميًا في `src/lib/notifications.functions.ts`:
  - أنواع `security.*` و`platform.*` وأي شدة `critical` تُعرض دائمًا باسم «ركيز» بشارة رسمية — لا يمكن لأي كيان انتحالها لأن الاشتقاق يتم على الخادم من `type_code`/`severity` لا من الحمولة.
  - غير ذلك: اسم الكيان يُقرأ من `entities` بصلاحية المستخدم نفسه (RLS)، وإن غاب يُستخدم `payload.actor_name` كمصدر «فرد»، وإلا يعود المصدر للمنصة.
- صفحة `src/routes/_authenticated/notifications.tsx`:
  - تبويبان: «غير المقروء (العدد)» و«الكل»، والعداد من `getUnreadCount` لا من طول القائمة.
  - القراءة لا تحذف السجل: يبقى في «الكل» مع «قُرئ في» بتوقيت الرياض وأرقام لاتينية `dir="ltr"`.
  - «تعليم الكل كمقروء» معطّل عند صفر غير مقروء.
  - إزالة أي مفاتيح إنجليزية خام: `typeLabel()` يعيد عبارة عربية لأي نوع غير معروف، مع تغطية أنواع المواعيد والمراسلات والتعاقد والتواصل.
- مفاتيح i18n جديدة (ar/en): previous, from, officialSource, readAt, unreadCount.

## التحقق
- `tsgo --noEmit`: نظيف.
- `vite build`: ناجح.
- اختبار متصفح: المسارات العامة تعمل، وتحويل غير المسجّل إلى `/auth` سليم بلا أخطاء console.

## ما تعذّر — بسبب حقيقي فقط
- **اختبار E2E مسجّل الدخول غير متاح**: حالة المصادقة في بيئة الفحص `external_unmanaged` (Supabase خارجي)، فلا يمكن توليد جلسة اختبار. لذلك وصول إشعار فعلي بين حسابين وعزلهما **غير مُتحقق منه تجريبيًا** في هذه الدفعة؛ العزل يعتمد على RLS القائم على `recipient_user_id`.
- **المهاجرات 01–07 ما زالت pending**: `SUPABASE_FORBIDDEN` يمنع DDL. لذلك دوال/أعمدة المواعيد (`appointment_card`, `reschedule_appointment`, `platform_references`) غير موجودة في الإنتاج، والرقم المرجعي وQR ما زالا معطّلين فعليًا لا شكليًا.
